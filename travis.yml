osx_image: xcode11.2
branches:
  only:
    - master
jobs:
  include:
    - stage: Run tests
      script: ./Scripts/test-macOS.sh
      os: osx
      osx_image: xcode11.2
    - stage: Release new version
      script: ./Scripts/new-release.sh
      os: osx
      osx_image: xcode11.2
      if: branch = master AND type = push AND fork = false
